{
  "name": "anagram-master",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web-app"
  ],
  "scripts": {
    "server:dev": "npm run dev --workspace server",
    "server:build": "npm run build --workspace server",
    "server:test": "npm run test --workspace server",
    "server:lint": "npm run lint --workspace server",
    "server:format": "npm run format --workspace server",
    "web:dev": "npm run dev --workspace web-app",
    "web:build": "npm run build --workspace web-app",
    "web:preview": "npm run preview --workspace web-app"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.94",
    "sharp": "^0.34.5",
    "to-ico": "^1.1.5"
  }
}
